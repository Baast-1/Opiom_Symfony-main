<form class="btn" method="post" action="{{ path('app_liste_event_delete', {'id': liste_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_event.id) }}">
    <button class="supprimer">Supprimer</button>
</form>
